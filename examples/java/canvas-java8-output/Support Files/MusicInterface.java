/**
 * Interface for the class students are implementing
 *
 * Can be helpful to make sure their method names, etc. are correct
 */
public interface MusicInterface<T> {

    // An example of a constant
    // This is a possible use case for using regexes to flag suspicious code (such as magic number use)
    // Check the config file to see how this is done
    static final int EXAMPLE_CONSTANT = 42;

    // Methods to test
    public String whatWeCouldHaveBeenTogether();
    public String whatWeCouldHaveLivedForever();
    public String whoIsGoingToDanceWithMe();
}
